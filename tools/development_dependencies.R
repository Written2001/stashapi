# Development-only packages used by repository tooling and the R editor.
requireNamespace("covr")
requireNamespace("languageserver")
requireNamespace("lintr")
requireNamespace("pkgdown")
requireNamespace("rcmdcheck")
