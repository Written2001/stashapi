% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/package_documentation.R
\name{stashapi_options}
\alias{stashapi_options}
\title{Common options for generated Stash operations}
\description{
Generated operations accept common options through their `...` arguments.
}
\details{
Use `.field` to select a nested response field, `.response` to choose the
response mode, and `.progress_bar = TRUE` to display an opt-in progress bar
while collecting paginated results. Progress bars are disabled by default.
}
\examples{
\dontrun{
findPerformers(
  filter = find_filter(per_page = -1),
  .progress_bar = TRUE
)

findPerformers(
  performerfilter = performer_filter(name = includes("Leah")),
  .field = c("performers", "id")
)

findPerformers(
  performerfilter = performer_filter(name = includes("Leah")),
  .response = "object"
)
}
}
